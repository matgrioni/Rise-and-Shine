<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grioni.app.screenwakecounter" >
    
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.grioni.app.screenwakecounter.ScreenCountService">
            <intent-filter>
                <action android:name="com.grioni.app.screenwakecounter.ScreenCountService" />
            </intent-filter>
        </service>

        <service
            android:name="com.grioni.app.screenwakecounter.ScreenCountWriteService">
            <intent-filter>
                <action android:name="com.grioni.app.screenwakecounter.ScreenCountWriteService" />
            </intent-filter>
        </service>
        
        <receiver android:name="com.grioni.app.screenwakecounter.ScreenWakeReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>


    </application>

</manifest>
